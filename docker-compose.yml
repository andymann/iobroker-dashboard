services:
  iobroker-dashboard:
    image: ghcr.io/andymann/iobroker-dashboard:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - config.env
    volumes:
      - ./states.txt:/app/states.txt:ro