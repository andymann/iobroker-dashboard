services:
  iobroker-dashboard:
    image: ghcr.io/GITHUB_USER/GITHUB_REPO:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - config.env
    volumes:
      - ./states.txt:/config/states.txt:ro